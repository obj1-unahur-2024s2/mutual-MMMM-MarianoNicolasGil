import example.*

describe "group of tests for pepita" {
  const ciudadTropical = new CiudadTropical(idiomas = ["espaniol", "ingles"], atracciones = 7)
  test "pepita has initial energy" {
    assert.equals(["espaniol", "ingles"], ciudadTropical.idiomas())
  }

}